<?php
// survey.php - Umfrageseite

// Namen aus GET-Parameter auslesen
$name = isset($_GET['name']) ? $_GET['name'] : '';

// Wenn Feld leer ist, anonym setzen
if (trim($name) === '') {
    $displayName = 'anonym';
    $userId = null; // später für Datenbank
} else {
    $displayName = htmlspecialchars($name, ENT_QUOTES, 'UTF-8');
    // $userId wird später aus der Datenbank ermittelt
}
?>
<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Umfrage - <?php echo $displayName; ?></title>
</head>
<body>

    <h1>Umfrage zum Studium</h1>
    
    <p>Teilnehmer: <strong><?php echo $displayName; ?></strong></p>
    
</body>
</html>